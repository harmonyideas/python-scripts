import timeit


def fib(n):
    i = 0
    first=0
    last = 1

    while i < n:
        yield last
        first, last = last, first + last
        i += 1


timer = timeit.Timer(stmt='for x in fib(10): print(x)', setup="from __main__ import fib")
print timer.timeit(number=1)


