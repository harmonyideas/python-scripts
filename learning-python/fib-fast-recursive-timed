import timeit


def fib(n, index = {0: 0, 1: 1}):
    if n not in index:
        index[n] = fib(n - 1, index) + fib(n - 2, index)
    return index[n]


timer = timeit.Timer(stmt='print fib(500)', setup="from __main__ import fib")
print timer.timeit(number=1)
